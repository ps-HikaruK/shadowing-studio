# Contributing

個人利用の MVP ですが、変更の単位を小さく保つためのルールです。

1. 1 つの Pull Request または変更単位では、1 つのユーザーフローだけを実装する
2. TTS 生成前に、必ず分割結果を確認・修正できるようにする
3. TTS API を直接 UI コンポーネントから呼ばない
4. 音声再生ロジックをプレーヤー UI から分離する
5. キャッシュヒット時には外部 API を呼ばないテストを書く
6. iPhone 実機確認なしで音声関連機能を完了扱いにしない
7. 範囲外の機能は Issue へ記録し、同時実装しない

プロキシ URL、共有トークン、API キーをリポジトリ、GitHub Actions の変数、Issue、スクリーンショットに含めないでください。
